function lastKNum(n, k) {

    let res = [];
    let index = 1;
    for (let i = 0; i < n; i++) {
        for (let j = 0; j < k; j++){
           console.log();
         
        }
        // res.push(index);
        // index = res[i] + 
    }
    console.log(res);
}
lastKNum(6, 3);